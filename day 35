\\Find the second largest element in an array.
#include <stdio.h>
#include <limits.h>
int main()
{
int n, a[100];
int largest = INT_MIN;
int second = INT_MIN;
scanf("%d", &n);
for (int i = 0; i < n; i++)
scanf("%d", &a[i]);
for (int i = 0; i < n; i++)
{
if (a[i] > largest)
{
second = largest;
largest = a[i];
}
else if (a[i] > second && a[i] != largest)
{
second = a[i];
}
}
if (second == INT_MIN)
printf("-1");
else
printf("%d", second);
return 0;
}


\\Rotate an array to the right by k positions.
  #include <stdio.h>
int main()
{
int n, a[100], temp[100], k;
scanf("%d", &n);
for (int i = 0; i < n; i++)
scanf("%d", &a[i]);
scanf("%d", &k);
k = k % n;
for (int i = 0; i < n; i++)
temp[(i + k) % n] = a[i];
for (int i = 0; i < n; i++)
printf("%d ", temp[i]);
return 0;
}
